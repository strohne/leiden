[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "leiden-parser"
version = "0.1.0"
dependencies = [
    "DHParser",
    "pandas",
    "openpyxl",
    "tqdm",
]

[tool.setuptools.packages.find]
include = ["parsers*"]
